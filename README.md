# 🅿️ Парсер парковок Санкт-Петербурга

Проект для автоматического сбора данных о парковках Санкт-Петербурга из Яндекс.Карт и 2ГИС с последующим объединением и анализом результатов.

### 📋 Возможности

* Парсинг Яндекс.Карт: Сбор данных о парковках с Яндекс.Карт
* Парсинг 2ГИС: Сбор данных о парковках с 2ГИС
* Автоматическое объединение: Сравнение и объединение данных из двух источников
* Гибкое управление: Возможность парсить по отдельности или одновременно
* Формирование отчетов: Детальные Excel-отчеты с двухстрочными заголовками
* Поддержка форматов: Сохранение в JSON и Excel
* Повторное объединение: Возможность объединить ранее сохраненные данные

### 📁 Структура проекта

```text
parking_parser/
├── main.py                    # Главный скрипт парсинга
├── merge_data.py              # Утилита объединения данных
├── README.md                  # Документация (этот файл)
├── requirements.txt           # Зависимости
├── config.py                  # Конфигурация
├── parsers/                   # Парсеры
│   ├── base_parser.py        # Базовый класс парсера
│   ├── yandex_parser.py      # Парсер Яндекс.Карт
│   └── twogis_parser.py      # Парсер 2ГИС
├── core/                      # Основная логика
│   ├── data_merger.py        # Объединение данных
│   └── excel_writer.py       # Создание Excel отчетов
├── utils/                     # Утилиты
│   ├── geoTools.py           # Географические утилиты
│   └── helpers.py            # Вспомогательные функции
└── results/                   # Результаты парсинга (создается автоматически)
```

### ⚙️ Установка и настройка

#### Клонирование репозитория

```bash
git clone <ваш-репозиторий>
cd parking_parser
```

#### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

**Основные зависимости:**

* nodriver - управление браузером
* pandas - работа с таблицами
* openpyxl - создание Excel файлов
* beautifulsoup4 - парсинг HTML
* lxml - парсинг XML/HTML

#### 3. Настройка (опционально)

Отредактируйте файл `config.py` при необходимости:

```python
# Настройки браузера
HEADLESS = False  # True - скрытый режим, False - видимый
BROWSER_TIMEOUT = 30  # Таймаут в секундах

# Настройки парсинга
MAX_PARKINGS = None  # Ограничение количества парковок (None - без ограничений)
COORD_TOLERANCE = 0.001  # Допуск по координатам для сравнения
```

### 🚀 Использование

#### Основной скрипт парсинга

1. Полный парсинг (Яндекс + 2ГИС)
```bash
python main.py
```
2. Только Яндекс Карты
```bash
python main.py --skip-2gis
```
3. Только 2ГИС
```bash
python main.py --skip-yandex
```
4. Headless режим (без отображения браузера)
```bash
python main.py --headless
```
5. Только объединение существующих данных
```bash
python main.py --merge-only
```
6. Объединение с указанием файлов
```bash
python main.py --merge-only --yandex-file results/parking_yandex_20240101_1200.json --twogis-file results/parking_2gis_20240101_1300.json
```

#### Утилита объединения данных

1. Автоматическое объединение (поиск последних файлов)
```bash
python merge_data.py --auto
```
2. Объединение с указанием файлов
```bash
python merge_data.py --yandex-file путь/к/yandex.json --twogis-file путь/к/2gis.json
```
3. Сокращенная версия
```bash
python merge_data.py -y yandex.json -t 2gis.json
```
4. Указать выходной файл
```bash
python merge_data.py -y yandex.json -t 2gis.json -o мои_парковки.xlsx
```
5. Показать доступные файлы
```bash
python merge_data.py --list-files
```

### 📊 Форматы файлов

#### JSON файлы

Парсер автоматически сохраняет данные в JSON формате:

* results/parking_yandex_YYYYMMDD_HHMMSS.json
* results/parking_2gis_YYYYMMDD_HHMMSS.json

**Структура JSON:**

```json
[
  {
    "Название объекта": "Парковка ТЦ",
    "Координаты": "59.123456,30.123456",
    "Адрес": "Санкт-Петербург, Невский пр., 1",
    "Телефон": "+79111234567",
    "Тип парковки": "платная, крытая",
    "Тарифы": "100 руб/час",
    "Время работы": "круглосуточно",
    "Вместимость": "50",
    "Оценка": "4.5",
    "Ссылка": "https://yandex.ru/maps/org/...",
    "timestamp": "2024-01-01 12:00:00"
  }
]
```

#### Excel файлы

Создается несколько файлов:

1. **Индивидуальные файлы** (простой формат):

- parking_yandex_YYYYMMDD_HHMMSS.xlsx

- parking_2gis_YYYYMMDD_HHMMSS.xlsx

2. **Объединенный файл** (детальный формат):

- parking_merged_YYYYMMDD_HHMMSS.xlsx

**Структура объединенного Excel:**

* Лист 1: Объединенные данные (двухстрочный заголовок)

  - Первая строка: названия полей

  - Вторая строка: источники данных (Яндекс/2ГИС)

  - Позволяет легко сравнивать данные из разных источников

* Лист 2: Яндекс Карты (оригинальные данные)

* Лист 3: 2ГИС (оригинальные данные)

* Лист 4: Сводка (статистика парсинга)

### 🎯 Особенности парсеров

#### Яндекс.Карт парсер

* Использует бесконечный скроллинг для загрузки всех результатов
* Автоматически кликает кнопку "Показать результаты"
* Собирает полную информацию о парковках:
* Название и адрес
* Координаты
* Телефоны и сайт
* Тип парковки (платная/бесплатная, открытая/закрытая)
* Тарифы и время работы
* Вместимость и рейтинг  Описание

#### 2ГИС парсер

* Парсит данные с сайта 2ГИС
* Собирает аналогичную информацию о парковках
* Интегрирован с основной системой сравнения

### 🔍 Логика объединения данных

Алгоритм объединения использует несколько критериев для поиска совпадений:

1. Координаты (самый важный критерий) - допуск 0.001 градуса
2. Название - схожесть текста ≥ 70%
3. Адрес - совпадение ключевых слов
4. Телефон - совпадение номеров

**Результаты объединения:**

* ✅ Совпадения: Данные из обоих источников объединяются
* 📍 Только Яндекс: Объекты найденные только в Яндекс.Картах
🗺* ️ Только 2ГИС: Объекты найденные только в 2ГИС

### ⚡ Производительность и оптимизация

#### Скроллинг Яндекс.Карт

Парсер использует умный алгоритм скроллинга:

* Останавливается после 3 попыток без новых данных
* Автоматически определяет конец списка
* Продолжает работать пока загружаются новые парковки

**Задержки и таймауты**

* Задержки между запросами: 2-4 секунды
* Таймаут загрузки страницы: 30 секунд
* Повторные попытки при ошибках: 2 раза

### 🐛 Устранение проблем

1. Браузер не запускается

```bash
# Проверьте установку nodriver
pip show nodriver

# Попробуйте обновить
pip install --upgrade nodriver
```

2. Нет новых данных при скроллинге

* Убедитесь, что интернет соединение стабильно
* Попробуйте увеличить задержки в config.py
* Запустите в видимом режиме (--headless=false) для отладки

3. Ошибки при сохранении Excel

```bash
# Установите openpyxl
pip install openpyxl

# Или обновите
pip install --upgrade openpyxl pandas
```

4. Мало данных парсинга

* Яндекс может ограничивать количество результатов
* Попробуйте запустить парсер в разное время суток
* Проверьте корректность поискового запроса в коде

### 📈 Анализ результатов

После парсинга вы получаете:

#### Статистика в консоли

```text
📊 ИТОГОВАЯ СТАТИСТИКА:
   Яндекс Карт: 350 объектов
   2ГИС: 280 объектов
   Объединено: 400 объектов
```

#### Статистика в Excel (лист "Сводка")

* Количество объектов по источникам
* Типы парковок (закрытые/охраняемые/платные)
* Процент совпадений между источниками
* Даты и время парсинга

### 🔄 Рабочий процесс

#### Рекомендуемый процесс работы:

1. Парсинг данных (утром, когда сервера менее нагружены):

```bash
python main.py --headless
```

2. Анализ результатов:

* Откройте results/parking_merged_...xlsx
* Изучите лист "Объединенные данные"
* Проверьте конфликты в колонке "Конфликт данных"

3. Дополнительное объединение (если нужно):

```bash
python merge_data.py --auto
```

#### Для регулярного сбора данных:

Создайте bash-скрипт или используйте планировщик задач:

```bash
# daily_parsing.sh
#!/bin/bash
cd /путь/к/parking_parser
python main.py --headless --skip-2gis
sleep 3600  # Ждем час
python main.py --headless --skip-yandex
python merge_data.py --auto
```

### 📝 Примечания

#### Ограничения

1. Яндекс.Карты: Может ограничивать количество отображаемых результатов
2. 2ГИС: Может требовать капчу при частых запросах
3. Скорость: Полный парсинг может занимать 1-2 часа
4. Память: При большом количестве данных (>1000 объектов) может потребоваться больше RAM

#### Рекомендации

1. Используйте --headless для автоматического парсинга
2. Сохраняйте JSON файлы для последующего анализа
3. Регулярно обновляйте зависимости
4. Проверяйте логи в консоли для отладки

#### Лицензия

Проект предназначен для образовательных и исследовательских целей. Используйте данные в соответствии с пользовательскими соглашениями источников.

Последнее обновление: Декабрь 2025<br>
Версия: 1.0.0<br>
Автор: Shkola Olga

